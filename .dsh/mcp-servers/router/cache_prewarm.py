# -*- coding: utf-8 -*-
"""缓存热身前缀 — 单一真相源 (2026-08-20 提取自 router_mcp_server)。

背景: DeepSeek 前缀缓存块粒度 ~2048 tokens。短查询 (system ~20 tokens) 永远
够不到门槛 → cache_hit=0 → 缓存省钱失效。方案: 固定、静态、业务无关的长前缀
注入 system, 使每次请求达到缓存块粒度。前缀内容永不变化 → 全局会话共享同一
缓存前缀。

引用方:
- router_mcp_server.py  (独立 MCP server, 平级 `import cache_prewarm`)
- compact_governance.py (core 内部, `from core.mcp_servers.cache_prewarm import ...`)

⚠️ 这是"热身"块: 永远不要在此块内放任何会随请求变化的内容, 否则缓存前缀分叉。
修改本文件 = 所有依赖方缓存前缀同时分叉 → 一次全冷。改前必三思。
"""

CACHE_PREWARM_PREFIX = (
    "系统运行规范与通用指令(缓存热身块, 内容静态不变)。\n"
    "本块用于确保每次请求的提示词前缀长度达到大模型提供商的缓存命中门槛, "
    "从而让重复的固定上下文享受缓存计费优惠, 降低整体调用成本。\n"
    "以下为固定的通用行为准则, 适用于所有类型的任务, 与具体业务无关:\n"
    "1. 输出应当准确、完整、结构清晰, 直接回应问题, 不堆砌无关内容。\n"
    "2. 涉及数据或事实时, 尽量引用来源或说明依据; 不确定时明确标注不确定。\n"
    "3. 回答使用与用户提问相同的语言; 涉及专有名词时保留原文并附中文解释。\n"
    "4. 长回答优先使用层级标题、列表、表格等结构化形式, 便于阅读。\n"
    "5. 需要逐步推理的问题, 先给出结论再展示关键步骤, 避免冗长过程。\n"
    "6. 涉及代码时, 确保语法正确、可运行, 并给出必要说明和边界处理。\n"
    "7. 遵循用户对输出格式、长度、风格的明确要求; 无明确要求时采用通用最佳实践。\n"
    "8. 当信息不足时, 主动说明缺失内容并给出获取途径建议, 不臆测。\n"
    "9. 安全优先: 不输出有害、误导或敏感内容; 涉及安全问题时明确风险提示。\n"
    "10. 任务完成后, 简要总结关键结论, 便于用户快速掌握要点。\n"
    "本块重复内容仅为满足缓存前缀长度需求, 请忽略其中的重复部分, "
    "直接执行用户的实际请求。以上为缓存热身块的固定内容, 永不修改。\n"
) * 24  # ×24 ≈ 5000+ tokens, 远超 2048 缓存门槛
